.sidebar_2
  .oblako
    form
    =form_tag(wall_index_path, method: "post", remote: true, :id => 'asdf') do
      =text_field_tag(:q)
      =submit_tag("Search")
    #qa
      -@wallList.each do |u|
        %h3=u.usr
        %h5=u.post
.clr