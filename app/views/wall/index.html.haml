%h1 Listing posts
%table
  %tr
    %th Title
    %th content
  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.content
      %td= link_to 'Show', wall_path(post)
      %td= link_to 'Edit', edit_wall_path(post)
      %td= link_to 'Destroy', wall_path(post)                 |
  = link_to 'New Post', new_wall_path

